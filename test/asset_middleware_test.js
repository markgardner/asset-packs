'use strict';

var path = require('path'),
	should = require('should'),
	sinon = require('sinon');

var asset_packs = require('../tasks/asset_packs'),
	AssetPacker = require('../lib/AssetPacker');

describe('asset_middleware_test', function() {
	beforeEach(function() {
	});

	afterEach(function() {
	});

	it('registers itself with grunt', function() {
	});
});